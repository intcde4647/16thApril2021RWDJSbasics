// anonymous function


// In JS the functions can be
// treated as object and can be
// assigned to a variable or 
// can be passed to a function
// or returned from a function
// depending on the usage

let addValues=function(num1,num2){
   return num1+num2;
}

console.log(addValues(3,5));
